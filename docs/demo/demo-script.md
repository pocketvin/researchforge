# ResearchForge V1.4 Demo Script / 演示脚本

## 中文（约 3 分钟）

1. 运行 `docker compose up -d --wait`，说明系统由 React、FastAPI 和 PostgreSQL 三个服务组成。
2. 在 Research 页选择研究模式、公司和报告期并提交。强调所有数字来自 API 的冻结事实，公式由 `Decimal` 确定性计算。
3. 展示 10 阶段 LangGraph 进度、执行摘要、财务快照、Claim—Fact—Evidence、反证搜索、限制和官方来源。
4. 打开 Skill Lab，读取 `experiment_contingency_v1_5_001`。说明两轮实验均因 `NO_ELIGIBLE_CLUSTER` 停止，没有 Candidate、Validation 或 Final Test；这是诚实的负研究结果。
5. 明确结尾：工程产品可以交付，但研究假设未获支持；这不是投资建议；真实用户价值尚未验证；三次评审始终标注 `SIMULATED`。

## English (about 3 minutes)

1. Start Docker Compose and identify the React, FastAPI, and PostgreSQL services.
2. Submit a mode, company, period, and question on Research. Explain that every number comes from frozen API facts and deterministic `Decimal` formulas.
3. Show the ten-stage LangGraph progress, executive summary, financial snapshot, Claim—Fact—Evidence links, counter-evidence, limitations, and official sources.
4. Open Skill Lab and load `experiment_contingency_v1_5_001`. Explain that both experiments stopped at `NO_ELIGIBLE_CLUSTER`; no Candidate, Validation, or Final Test was produced.
5. Close honestly: the engineering product can ship, but the research hypothesis is unsupported; this is not investment advice; real-user value remains unvalidated; simulations are labeled `SIMULATED`.

## Recorded Asset

`docs/assets/researchforge-v1.4-final-demo.mp4` is a silent 12-second product preview generated from the two verified screenshots. It is a repository preview, not a substitute for the reproducible live walkthrough above.
