# fundamental-research

This directory contains immutable versions of ResearchForge's one bounded Research Skill. Runtime configuration selects a version and content hash explicitly; files under `versions/` are never edited after a formal run references them.

The skill defines research procedure and coverage. It does not contain provider credentials, company-specific conclusions, Benchmark labels, verifier ground truth, model routing, graph topology, or financial formulas.
